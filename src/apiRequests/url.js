const HOST_NAME = 'api.appilizer.com'
// const HOST_NAME = 'localhost:3001'
const ROUTE = 'frontend'

// General URL for Front-end App
export const URL = `https://${HOST_NAME}/${ROUTE}`

// URL to get all the apps
export const saveFormUrl = `${URL}/saveForm`

// // URL to get Api Key
// export const getApiKeyUrl = `${URL}/authenticate`

